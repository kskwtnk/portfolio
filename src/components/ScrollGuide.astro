<script>
  function stopSmilAnimations() {
    let svgs = document.querySelectorAll("svg");
    svgs.forEach((svg) => {
      svg.pauseAnimations();
    });
  }

  let reducedMotion =
    window.matchMedia(`(prefers-reduced-motion: reduce)`).matches === true;

  if (reducedMotion) {
    stopSmilAnimations();
  }
</script>

<div class="flex items-center gap-2 text-xs text-zinc-950">
  <svg width="18" height="25" viewBox="0 0 18 25">
    <defs>
      <symbol id="arrow">
        <path
          d="M1 0l-1 1c3 1 7 4 9 6c2 -2 6 -5 9 -6l-1 -1c-4 1 -8 4 -8 4c0 0 -4 -3 -8 -4Z"
        >
          <animateMotion dur="4s" repeatCount="indefinite" path="M0 0 V18"
          ></animateMotion>
        </path>
      </symbol>
    </defs>
    <use href="#arrow" y="-18"></use>
    <use href="#arrow" y="-9"></use>
    <use href="#arrow" y="0"></use>
    <use href="#arrow" y="9"></use>
    <use href="#arrow" y="18"></use>
  </svg>
  <span>下へスクロール</span>
</div>
